import React from "react";
import Profile from "../Profile/Profile";
import "./Nav.css";

const Nav = ({ user }) => {
    return (
        <div className="homeNav">
            Nav
        </div>
    );
}

export default Nav;